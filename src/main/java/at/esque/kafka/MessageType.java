package at.esque.kafka;

public enum MessageType {
    STRING,
    AVRO,
    AVRO_TOPIC_RECORD_NAME_STRATEGY,
    SHORT,
    INTEGER,
    LONG,
    FLOAT,
    DOUBLE,
    BYTEARRAY,
    BYTEBUFFER,
    BYTES,
    UUID,
    PROTOBUF_SR
}
